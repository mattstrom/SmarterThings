{
  "name": "smartthings-keypad",
  "version": "0.0.0",
  "license": "MIT",
  "config": {
    "dockerTag": "smartthings-dispatcher-db"
  },
  "scripts": {
    "start": "docker run -p 27017:27017 -it $npm_package_config_dockerTag",
    "build": "docker build -t $npm_package_config_dockerTag ."
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {}
}
